# Pinned to the versions the model was trained and evaluated with.
# For GPU, install torch/torchvision from the CUDA index instead:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121

ultralytics==8.3.180
torch==2.5.1
torchvision==0.20.1
streamlit==1.48.1
opencv-python==4.12.0.88
numpy==2.2.6
pillow==11.3.0
